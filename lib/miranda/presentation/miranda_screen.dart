import 'package:flutter/material.dart';

class MirandaScreen extends StatelessWidget {
  const MirandaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Miranda Screen')),
    );
  }
}
